---
name: easy-xianyu-title-copy-free
description: 免费、免第三方 API Key 的闲鱼标题文案助手。用户有商品资料、图片或现有草稿，想生成更清楚的标题、描述、卖点和搜索词，并检查明显敏感或夸大表达时使用。它只输出文案和检查报告，不自动发布、不保证曝光或成交。
---

# 免费闲鱼标题文案助手

把商品真实信息改写成容易看懂的闲鱼标题和描述。先问清楚商品是什么、成色和价格，不编造参数、品牌授权、销量或承诺。

## 适用场景

- 需要 3 至 5 组不同角度的闲鱼标题。
- 需要把零散资料整理成商品描述、规格、交易说明和搜索词。
- 已有闲鱼草稿，想找出夸大、重复或可能触发审核的词。

## 输入

```json
{
  "product": {
    "name": "九成新机械键盘",
    "facts": ["87 键", "有线", "含键帽"],
    "condition": "二手九成新",
    "price": "128",
    "audience": "办公和入门玩家"
  },
  "style": "清楚实用",
  "variants": 3
}
```

至少提供商品名称和两条真实事实；价格未知就写“待补”，不自行估价。若要根据闲鱼页面校对字段，额外提供已登录 Easy WebBridge 的 `browserId` 和账号别名。

## 操作步骤

1. 把输入拆成已知事实、用户主观描述和缺失字段，缺失字段先列出。
2. 生成标题候选：商品词在前，关键规格、成色、用途或交易方式按真实资料排序；不堆无关热词。
3. 生成描述：先说商品和状态，再列规格、瑕疵、配件、发货/自取和沟通事项；对未知内容使用“待确认”。
4. 运行本地词语检查，标出绝对化承诺、虚假稀缺、疑似违规交易和无法证明的品牌/效果描述，给出替代表达，不擅自删除真实关键信息。
5. 输出多版本文案和修改说明；若连接浏览器，只读取目标草稿做比对，不点击发布或发送。

## 输出

输出 JSON 或 Markdown，包含 `titleVariants`、`description`、`sellingPoints`、`searchTerms`、`missingFacts`、`flaggedPhrases`、`disclaimers` 和 `nextAction`。每个标题都应能单独使用，并标记使用了哪些事实。

## 失败处理

- 资料过少：返回 `needs_user_input` 和最少补充清单，不凭空补全。
- 事实互相矛盾：分别列出冲突，不替用户选择版本。
- 用户要求保证成交、规避平台审核或伪造品牌/成色：拒绝该部分，提供真实信息写法。
- 浏览器未启动或账号未登录：文案任务仍可离线完成；页面比对标记 `needs_user_action`。

## 安全边界

不保存账号信息、Cookie、密码、Token 或个人资料；不自动上架、改价、私信、下架或删除。免 API Key 不等于免登录。

## 自测

```bash
node scripts/self-test.mjs
```

